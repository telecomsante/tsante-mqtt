version: "2"

services:
  mqtt:
    image: jllopis/mosquitto:v1.4.10
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "1m"
        max-file: "5"
    volumes:
      - ./config:/etc/mosquitto
    ports:
      - 1883:1883
      - 9883:9883
